# Core framework
Django>=5.0,<5.1
djangorestframework>=3.15
drf-spectacular>=0.27

# Multi-tenancy (schema-per-tenant, per technical.md §3)
django-tenants>=3.7

# Database
psycopg2-binary>=2.9

# Async / background jobs (technical.md §2)
celery>=5.4
redis>=5.0
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# Filtering / API ergonomics
django-filter>=24.2

# State machines (PendingApproval lifecycle, technical.md §8.4)
django-fsm>=2.8

# Env/config loading
django-environ>=0.11

# CORS for local Vite dev server talking to Django
django-cors-headers>=4.4
